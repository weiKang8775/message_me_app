<% flash.each do |name, msg| %>
  <%= content_tag :div, class: ["ui", name, "message"] do %>
    <i class="close icon"></i>
    <div class="header">
      <%= name %>
    </div>
    <%= msg %>
  <% end %>
<% end %>